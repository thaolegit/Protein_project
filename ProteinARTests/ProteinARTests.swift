//
//  ProteinARTests.swift
//  ProteinARTests
//
//  Created by Thao P Le on 24/09/2020.
//  Copyright © 2020 Thao P Le. All rights reserved.
//

/*import XCTest
@testable import Protein_v3_1
 



class ProteinARTests: XCTestCase {
    
    var sut: EduViewController!

    override func setUp(){
        // Put setup code here. This method is called before the invocation of each test method in the class.
        super.setUp()
        sut = EduViewController()
    }

    override func tearDown() {
        // Put teardown code here. This method is called after the invocation of each test method in the class.
        sut = nil
        super.tearDown()
    }

    func testDownload() {
        // Use XCTAssert and related functions to verify your tests produce the correct results.
        //1. given
        let fileURL = URL(string: "https://files.rcsb.org/download/6K00.pdb")
        let parameter = "6K00"
       
        //2. when
        sut.download(fileURL: fileURL, parameter: parameter)
        
        //3.then
    }

    func testPerformanceExample() throws {
        // This is an example of a performance test case.
        measure {
            // Put the code you want to measure the time of here.
        }
    }

 
 class ProteinARTests: XCTestCase {
     
     var sut: EduViewController!

     override func setUp(){
         // Put setup code here. This method is called before the invocation of each test method in the class.
         super.setUp()
         sut = EduViewController()
     }

     override func tearDown() {
         // Put teardown code here. This method is called after the invocation of each test method in the class.
         sut = nil
         super.tearDown()
     }

     func testDownload() {
         // Use XCTAssert and related functions to verify your tests produce the correct results.
         //1. given
         let fileURL = URL(string: "https://files.rcsb.org/download/6K00.pdb")
         let parameter = "6K00"
        
         //2. when
         sut.download(fileURL: fileURL, parameter: parameter)
         
         //3.then
     }

     func testPerformanceExample() throws {
         // This is an example of a performance test case.
         measure {
             // Put the code you want to measure the time of here.
         }
     }

 }

}*/
